exports.newUIFileServer = function newUIFileServer() {

    const MODULE = "UI File Server"

    let thisObject = {
        initialize: initialize,
        finalize: finalize,
        run: run,
        name: 'UI File Server'
    }

    return thisObject

    function finalize() {

    }

    function initialize() {

    }

    function run() {
        
     }
}