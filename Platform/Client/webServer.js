exports.newWebServer = function newWebServer() {

    const MODULE = "Web Server"

    let thisObject = {
        initialize: initialize,
        finalize: finalize,
        run: run,
        name: 'Web Server'
    }

    return thisObject

    function finalize() {

    }

    function initialize() {

    }

    function run() {
        
     }
}