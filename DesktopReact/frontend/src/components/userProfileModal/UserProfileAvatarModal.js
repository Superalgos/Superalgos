import React from 'react';

const UserProfileAvatarModal = (props) => {

    const {
        userInfo,
        handleChange,
        selectProfilePic,
        selectBannerPic,
        saveProfile,
        isEquals,
        errorState,
        close,
        modalEditAvatar
    } = props;

    return (
        <div>

        </div>

    );
};

export default UserProfileAvatarModal;